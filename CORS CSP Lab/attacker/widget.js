/**
 * The script loader.js fetches when it finds a data-main attribute pointing
 * here. It carries no nonce and its host is nowhere in the policy; under
 * 'strict-dynamic' neither of those things matters.
 */
hlab.win('attacker/widget.js, inserted by loader.js under strict-dynamic');
